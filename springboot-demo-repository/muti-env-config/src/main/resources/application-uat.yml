info:
  build:
    name: @project.artifactId@
    groupId: @project.groupId@
    artifactId: @project.artifactId@
    version: @project.version@

server:
  port: 8084

endpoints:
  enabled: true
  sensitive: false

data:
  test:
    envName: uat
    envconfig: 127.0.0.1:8084